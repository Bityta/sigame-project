# SIGame - Online Quiz Game

Многопользовательская онлайн-игра в формате викторины на базе микросервисной архитектуры.

## 🚀 Быстрый старт для деплоя

```bash
# Обновить код на сервере
bash deployment/scripts/deploy.sh
```

**Сервер**: http://89.169.139.21  
**SSH**: `ssh ubuntu@89.169.139.21`  
**Мониторинг**: http://89.169.139.21:3000 (Grafana)

## 📦 Архитектура

- **Frontend**: React + TypeScript + Vite
- **Backend Services**:
  - **Auth**: Go (аутентификация/авторизация)
  - **Lobby**: Kotlin (управление комнатами)
  - **Game**: Go (игровая логика + WebSocket)
  - **Pack**: Python (управление паками вопросов)
- **Infrastructure**:
  - PostgreSQL (4 базы данных)
  - Redis (кэш и сессии)
  - MinIO (S3-совместимое хранилище)
  - Kafka (очереди сообщений)
  - Prometheus + Grafana (мониторинг)

## 🛠️ Локальная разработка

```bash
# Клонировать репозиторий
git clone https://github.com/Bityta/sigame-project.git
cd sigame-project

# Запустить инфраструктуру
docker compose -f docker-compose.infra.yml up -d

# Запустить приложения
docker compose -f docker-compose.app.yml up -d

# Открыть в браузере
open http://localhost:3000
```

## 📋 Требования

- Docker 24.0+
- Docker Compose 2.20+
- Git 2.30+

## 🔧 Структура проекта

```
sigame-project/
├── frontend/              # React фронтенд
├── services/
│   ├── auth/             # Auth сервис (Go)
│   ├── lobby/            # Lobby сервис (Kotlin)
│   ├── game/             # Game сервис (Go)
│   └── packs/            # Pack сервис (Python)
├── infrastructure/        # Конфигурация инфраструктуры
├── deployment/           # Скрипты и конфиги для деплоя
│   ├── scripts/         # Bash скрипты
│   └── terraform/       # IaC для Yandex Cloud
└── docker-compose*.yml  # Docker Compose файлы
```

## 📝 Лицензия

MIT
